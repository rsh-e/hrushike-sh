# dependencies
/node_modules

# build output (Netlify builds fresh; keep local dist out of git noise if you prefer — we commit optional)
# /dist

# misc
.DS_Store
*.pem
.env*
.vercel

# debug
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*
